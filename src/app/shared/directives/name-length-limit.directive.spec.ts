import { NameLengthLimitDirective } from './name-length-limit.directive';

describe('NameLengthLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new NameLengthLimitDirective();
    expect(directive).toBeTruthy();
  });
});
